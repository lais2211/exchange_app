import 'package:flutter/material.dart';

class Spacer32 extends StatelessWidget {
  const Spacer32({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 32);
  }
}